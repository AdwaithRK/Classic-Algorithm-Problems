#include <bits/stdc++.h>

using namespace std;

int main()
{
    string A = "adwaith";
    string B = "aditya";

    vector<vector<int>> DP(A.size() + 1, vector<int>(B.size() + 1));

    for (int i = 0; i <= A.size();)
}